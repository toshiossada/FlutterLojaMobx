import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:loja/app/modules/products/page/product/product_page.dart';

void main() {
  testWidgets('ProductPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProductPage(title: 'Product')));
    //  final titleFinder = find.text('Product');
    //  expect(titleFinder, findsOneWidget);
  });
}
