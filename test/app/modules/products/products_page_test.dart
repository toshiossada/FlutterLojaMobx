import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:loja/app/modules/products/products_page.dart';

void main() {
  testWidgets('ProductsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProductsPage(title: 'Products')));
    //  final titleFinder = find.text('Products');
    //  expect(titleFinder, findsOneWidget);
  });
}
