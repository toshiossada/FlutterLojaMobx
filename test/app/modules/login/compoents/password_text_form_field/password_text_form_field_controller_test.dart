import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:loja/app/modules/login/compoents/password_text_form_field/password_text_form_field_controller.dart';
import 'package:loja/app/modules/login/login_module.dart';

void main() {
  initModule(LoginModule());
  // PasswordTextFormFieldController passwordtextformfield;
  //
  setUp(() {
    //     passwordtextformfield = LoginModule.to.get<PasswordTextFormFieldController>();
  });

  group('PasswordTextFormFieldController Test', () {
    //   test("First Test", () {
    //     expect(passwordtextformfield, isInstanceOf<PasswordTextFormFieldController>());
    //   });

    //   test("Set Value", () {
    //     expect(passwordtextformfield.value, equals(0));
    //     passwordtextformfield.increment();
    //     expect(passwordtextformfield.value, equals(1));
    //   });
  });
}
