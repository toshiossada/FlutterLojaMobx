import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:loja/app/shared/store/user_store.dart';
import 'package:loja/app/app_module.dart';

void main() {
  initModule(AppModule());
  // UserStore user;
  //
  setUp(() {
    //     user = AppModule.to.get<UserStore>();
  });

  group('UserStore Test', () {
    //   test("First Test", () {
    //     expect(user, isInstanceOf<UserStore>());
    //   });

    //   test("Set Value", () {
    //     expect(user.value, equals(0));
    //     user.increment();
    //     expect(user.value, equals(1));
    //   });
  });
}
