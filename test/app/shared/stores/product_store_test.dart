import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:loja/app/shared/stores/product_store.dart';
import 'package:loja/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ProductStore product;
  //
  setUp(() {
    //     product = AppModule.to.get<ProductStore>();
  });

  group('ProductStore Test', () {
    //   test("First Test", () {
    //     expect(product, isInstanceOf<ProductStore>());
    //   });

    //   test("Set Value", () {
    //     expect(product.value, equals(0));
    //     product.increment();
    //     expect(product.value, equals(1));
    //   });
  });
}
