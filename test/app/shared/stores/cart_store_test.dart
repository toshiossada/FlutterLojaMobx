import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:loja/app/shared/stores/cart_store.dart';
import 'package:loja/app/app_module.dart';

void main() {
  initModule(AppModule());
  // CartStore cart;
  //
  setUp(() {
    //     cart = AppModule.to.get<CartStore>();
  });

  group('CartStore Test', () {
    //   test("First Test", () {
    //     expect(cart, isInstanceOf<CartStore>());
    //   });

    //   test("Set Value", () {
    //     expect(cart.value, equals(0));
    //     cart.increment();
    //     expect(cart.value, equals(1));
    //   });
  });
}
