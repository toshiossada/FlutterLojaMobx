import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:loja/app/shared/components/custom_drawer/custom_drawer_controller.dart';
import 'package:loja/app/app_module.dart';

void main() {
  initModule(AppModule());
  // CustomDrawerController customdrawer;
  //
  setUp(() {
    //     customdrawer = AppModule.to.get<CustomDrawerController>();
  });

  group('CustomDrawerController Test', () {
    //   test("First Test", () {
    //     expect(customdrawer, isInstanceOf<CustomDrawerController>());
    //   });

    //   test("Set Value", () {
    //     expect(customdrawer.value, equals(0));
    //     customdrawer.increment();
    //     expect(customdrawer.value, equals(1));
    //   });
  });
}
