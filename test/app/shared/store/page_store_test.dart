import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:loja/app/shared/store/page_store.dart';
import 'package:loja/app/app_module.dart';

void main() {
  initModule(AppModule());
  // PageStore page;
  //
  setUp(() {
    //     page = AppModule.to.get<PageStore>();
  });

  group('PageStore Test', () {
    //   test("First Test", () {
    //     expect(page, isInstanceOf<PageStore>());
    //   });

    //   test("Set Value", () {
    //     expect(page.value, equals(0));
    //     page.increment();
    //     expect(page.value, equals(1));
    //   });
  });
}
